# API Configuration
# The base URL for the backend API
# Default: http://localhost:8091/api/v1 for local development
NEXT_PUBLIC_API_URL=http://localhost:8091/api/v1

# NextAuth Configuration
# Required: Generate a secret with: openssl rand -base64 32
# Or use: npx auth secret
AUTH_SECRET=your-secret-key-here
